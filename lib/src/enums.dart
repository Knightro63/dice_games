enum Callbacks{allowDice,changeName,totalPlayers,computerSetScore,computerSetSelected,reRoll,gameType,playerType,unfocus,mainMenue}
enum GameType{none,yahtzee,farkle}
enum PlayerType{none,single,multi}
enum PlayerMove{roll,score}